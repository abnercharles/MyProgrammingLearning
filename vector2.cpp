#include <iostream>
#include <vector>

using namespace std;

int main()
{
    vector<int> vec(9,4);
    cout << vec [2];
   /* vec.push_back(3);
    vector<int>::iterator it;
    for (it = vec.begin();it != vec.end; ++it;)
        cout << *it << endl;*/
}